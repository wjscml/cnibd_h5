.image-enter-active
  transform translateX(0)
  transition all 1.5s ease
.image-leave-active
  transform translateX(-100%)
  transition all 1.5s ease
.image-enter
  transform translateX(100%)
.image-leave
  transform translateX(0)